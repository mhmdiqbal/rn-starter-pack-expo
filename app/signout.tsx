import { tw } from "@/lib/tailwind";
import { View } from "react-native";

const SignInScreen = () => {
    return (
        <View style={tw`flex-1 items-center justify-center bg-primary-500`}></View>
    )
}

export default SignInScreen;